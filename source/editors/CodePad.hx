package editors;

import flixel.FlxG;

// a state allows u coding inside the game, wow!
class CodePad extends MusicBeatState
{
  var str:String = "write code";
  
  override function create()
  {
    super.create();
  }

  override function update(elapsed:Float)
  {
    super.update(elapsed);
  }
}
